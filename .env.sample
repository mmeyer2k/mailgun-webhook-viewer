MONGODB_URI=mongodb://localhost:27017/mailgun-webhooks
PORT=3000
MAILGUN_API_KEY=your-mailgun-api-key-here

# Absolute origin of the tailnet-only viewer, used ONLY to render the
# "this address has moved" notice on the public origin. Leave empty to
# disable that notice (it then 404s). Example: https://host.example:8081
VIEWER_BASE_URL=
